(* Not finished because I want to focus on other exercices giving new knowledge
 * about OCaml.
*)

type 'a btree = Nil | Node of 'a * 'a btree * 'a btree

let insert_avl avl value () = avl

let delete_avl avl value () = avl
